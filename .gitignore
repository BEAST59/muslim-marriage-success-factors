# Editors & caches
.vscode/
.venv/
__pycache__/
*.pyc

# Raw/large files we don't want
data/*.xlsx    # ignore excel dumps
*.log          # ignore logs
*.tmp          # temp files

# Models (can be big, keep only if needed)
models/

# Data — allow important CSVs
!data/sample_divorce_data.csv
!data/column_labels.json
!data/divorce_data.csv
!data/primary_survey.csv

# Reports — allow all reports
!reports/**

# Envs & secrets
.env
